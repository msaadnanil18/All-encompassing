import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const filesReview = () => {
  return (
    <View>
      <Text>filesReview</Text>
    </View>
  );
};

export default filesReview;

const styles = StyleSheet.create({});
